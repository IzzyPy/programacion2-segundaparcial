// build.gradle (archivo de la raíz del proyecto)

plugins {
    id 'com.android.application' version '8.13.0' apply false
    // Puede que tengas otras líneas aquí, como la de com.android.library
}
